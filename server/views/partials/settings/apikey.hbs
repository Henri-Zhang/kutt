<section id="apikey-wrapper">
  <h2>API</h2>
  <p>
    除了使用本网站外，你还可以使用 API 来创建、删除和获取短链接。
    如果你不熟悉 API，请不要生成密钥。
    切勿将此密钥分享在你网站的客户端。
    <a href="https://docs.kutt.to" title="API 文档" target="_blank">
      阅读 API 文档。
    </a>
  </p>
  <div id="apikey">
    {{#if user.apikey}}
      <div class="clipboard small">
        <button
          type="button"
          aria-label="复制"
          hx-on:click="handleShortURLCopyLink(this);"
          data-url="{{user.apikey}}"
        >
        {{> icons/copy}}
        </button>
        {{> icons/check}}
      </div>
      <p
        hx-on:click="handleShortURLCopyLink(this);"
        data-url="{{user.apikey}}"
      >
        {{user.apikey}}
      </p>
    {{/if}}
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
  <form
    hx-post="/api/auth/apikey"
    id="generate-apikey"
    hx-target="#apikey-wrapper"
    hx-swap="outerHTML"
  >
    <button type="submit" class="secondary">
      <span>{{> icons/zap}}</span>
      <span>{{> icons/spinner}}</span>
      {{#if user.apikey}}重新生成{{else}}生成{{/if}}密钥
    </button>
  </form>
</section>
